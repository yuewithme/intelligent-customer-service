# Public production build settings only. API authentication is injected by
# the server-side Nginx proxy and must never be compiled into the browser app.
VITE_BASE_URL=
VITE_BASE_PATH=/
VITE_OUT_DIR=dist-prod
VITE_SOURCEMAP=false
VITE_REQUEST_TIMEOUT=180000
