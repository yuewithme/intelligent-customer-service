# Public development build settings only. Put developer overrides in the
# ignored .env.local file and never place backend credentials in VITE_* values.
VITE_BASE_URL=
VITE_BASE_PATH=/
VITE_PORT=5173
VITE_OUT_DIR=dist
VITE_SOURCEMAP=true
VITE_REQUEST_TIMEOUT=180000
