# 04 工具清单与渐进式披露

工具负责提供真实能力，不负责规定销售流程。Agent 自主决定是否调用；系统负责参数、权限、幂等和执行状态。

## 双层工具描述规范

每个工具有两层说明：

1. `short_description`：始终可见，一到两句话，说明“什么时候用”和最重要的边界。
2. `full_instructions`：Agent 选择相关能力后才加载，包含完整参数、结果、失败和副作用。

短描述示例：

```text
product.search
当客户需求可能对应可售商品时查询真实商品、价格和适用场景。普通养护问题且不涉及选品时不要调用。
```

## 当前预计的能力清单

请先修改能力名称和范围，不需要写技术参数。

| 能力 | 预期用途 | 当前系统是否已有 | 你需要补充 |
| --- | --- | --- | --- |
| customer.get_context | 获取客户历史、购买、承诺和待办 | 部分已有 | 希望 Agent 看见什么 |
| product.search | 查询真实商品和适配信息 | 已有产品 / 有赞能力 | 选品标准和返回重点 |
| product.get | 获取某个商品完整事实 | 部分已有 | 必要字段 |
| product.send_card | 发送已确认的商品卡片 | 部分已有 | 允许条件 |
| care_manual.search | 搜索品种或问题对应的养护手册 | 已有 | 检索维度 |
| material.search | 搜索可用销售资料 | 已有部分资料能力 | 资料分类和适用对象 |
| material.send | 发送已查询到的资料卡片 | 已有 | 资料经营原则 |
| order.lookup | 查询订单、付款或物流 | 已有有赞只读能力 | 身份匹配要求 |
| memory.record | 记录稳定客户事实、承诺和待办 | 已有长期记忆 | 哪些值得记住 |
| wakeup.schedule | 创建未来重新判断事件 | 已有 SOP / 定时能力可改造 | 唤醒场景 |
| human.handoff | 转人工并携带上下文 | 已有 | 转人工条件 |
| capability.search | 搜索并加载其他工具或经验 | 需要新增 | 目录分类 |

## 每个工具都要填写一张卡

复制下面模板，为每个工具填写一份。业务方先填写自然语言部分，参数 Schema 由开发实现。

### 工具卡模板

#### 基本信息

- 工具名称：`【请填写】`
- 客户或销售目标：
- `short_description`：

#### 什么时候使用

- （填写）
- （填写）

#### 什么时候不要使用

- （填写）
- （填写）

#### 调用前必须已经知道什么

- （填写）

#### Agent 需要提供的输入

| 输入 | 业务含义 | 必填 | 不知道时怎么办 |
| --- | --- | --- | --- |
|  |  |  |  |

#### 工具应该返回什么

| 输出 | 业务含义 | Agent 可以如何使用 |
| --- | --- | --- |
|  |  |  |

#### 真实引用

- 是否返回稳定引用，例如 `product_ref`、`material_ref`、`order_ref`：
- 哪些后续工具必须使用该引用：
- 引用多久失效：

#### 副作用和权限

- 这是只读工具还是写操作：
- 会产生什么真实动作：
- 是否需要人工批准：
- 是否允许重复调用：

#### 失败时

- 未找到时返回什么：
- 暂时失败时返回什么：
- Agent 可以如何向客户解释：
- Agent 绝对不能声称什么：

#### 正例和反例

- 应调用示例：
- 间接表达但应调用示例：
- 不应调用示例：
- 容易与哪个工具混淆：

## 第一轮请你填写

【请填写】第一版最需要 Agent 自主使用的五个工具，按优先级排序。

1. （填写）
2. （填写）
3. （填写）
4. （填写）
5. （填写）

【请填写】还有哪些当前系统能力没有出现在上表？

- （填写）
- （填写）
- （填写）
